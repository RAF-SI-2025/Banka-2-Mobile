<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- R6 deep2b: ACCESS_NETWORK_STATE uklonjen — nije bio koriscen (nema
         ConnectivityManager/NetworkCallback offline-detekcije). Kad/ako se doda
         offline mod, vrati ovu permisiju zajedno sa implementacijom. -->
    <!-- R3-1635 (feature backlog): push notifikacije (FCM) nisu implementirane —
         nema FirebaseMessagingService / POST_NOTIFICATIONS / notif kanala / deep-link
         iz push-a. In-app notif inbox (Routes.Notifications) pokriva trenutne potrebe;
         FCM je posebna feature stavka, ne P3 cleanup. -->

    <application
        android:name=".BankaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Banka2Mobile"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Banka2Mobile.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
